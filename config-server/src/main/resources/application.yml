server:
  port: 8081

spring:
  application:
    name: config-server

cloud:
  config:
    server:
      git:
        uri: 
        searchPaths: ${GIT_PASSWORD}
        username: ${GIT_USER}
        password:

